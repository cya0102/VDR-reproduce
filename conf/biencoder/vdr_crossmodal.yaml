max_len: 512
shared_encoder: False
device:

encoder_q:
  type: vdr_crossmodal_text
  model_id: bert-base-uncased
  max_len: 128
  shift_vocab_num: 999
  topk: 500
  pooling: max
  pooling_topk: 

encoder_p:
  type: vdr_crossmodal_image
  tokenizer_id: bert-base-uncased
  max_len: 256
  topk: 500
  width: 500
